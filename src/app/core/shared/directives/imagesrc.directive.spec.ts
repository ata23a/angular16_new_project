import { ImagesrcDirective } from './imagesrc.directive';

describe('ImagesrcDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesrcDirective();
    expect(directive).toBeTruthy();
  });
});
